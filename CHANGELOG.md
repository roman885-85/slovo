# Історія змін

## 1.2.9 — 23 вересня 2026

- **Панель фонограм згортається й розгортається плавно** (0,22 с, м'який
  хід), і подвійне клацання по межі — теж. Звичайне перетягування йде за
  рукою, без плавності.

## 1.2.8 — 23 вересня 2026

- **Рядки куплетів не лишаються порожніми**: перерахунок висот винесено з
  розкладки таблиці, а самі висоти міняються миттєво, без плавності, і
  список одразу перемальовується.

## 1.2.7 — 23 вересня 2026

- **Куплети не розтягуються після перетягування межі панелі фонограм.**
  Зміна ширини списку скидала виміряні висоти, а наново їх ніхто не міряв:
  клітинки вже збудовані. Тепер видимі рядки міряються по тексту, з
  повторним заходом.

## 1.2.6 — 23 вересня 2026

- **`window-snapshot` розповідає і про висоти рядків** куплетів: дано,
  треба, запам'ятовано — по кожному видимому.
- **`backing-drag`** — посунути межу панелі фонограм ззовні.
- **Перевірка «Куплети в живому вікні стоять за текстом»**: міряє рядки в
  головному вікні, а не в зібраному для перевірки.

## 1.2.5 — 23 вересня 2026

- **Знімок вікна на вимогу** (команда пульта `window-snapshot`): програма
  малює своє вікно у файл — для розбору поломок на чужій машині.
- **Перевірка «Куплети після перетягування панелі фонограм»**: межу панелі
  рухають чотири рази й щоразу міряють рядки куплетів.

## 1.2.4 — 23 вересня 2026

- **Куплети більше не займають зайвого місця.** Список скидав усі вже
  виміряні висоти щоразу, коли ширина мінялася посеред міряння — а вона
  міняється тоді, коли на список стає смуга прокрутки. Майже всі рядки
  лишалися з чорновою висотою у 86 точок при тексті в один рядок. Знайдено
  просто на машині служіння (macOS 11), де це й було видно.
- **Ширина для міряння — вужча з двох**: стовпець і видима область.
- **Самоперевірка міряє намальоване, а не чорнове**: прогін «куплети» гортає
  сторінку за сторінкою й міряє кожну одразу.

## 1.2.3 — 21 вересня 2026

- **Веб-слайди працюють на чистій машині.** Сервер сторінок не вмикався
  зовсім, якщо в теці даних не було теки `RemoteAPI`: канал працював, а
  сторінок не було («не работают веб слайды, даже локально»). Тепер тека
  заводиться сама, а свої сторінки програма малює кодом.
- **У переліку сторінок — тільки те, що справді віддається**: чужі імена з
  давніх налаштувань вели в «404».
- **Куплети не займають зайвого місця на macOS 11**: висоту рядка міряли по
  ширині стовпця, а малювали по ширині клітинки — там вона вужча.
- **Ім'я старої програми прибрано з написів і коментарів**; лишилося тільки
  там, де це формат файлів на диску.
- **Нова самоперевірка «куплети»** — прогін по всіх збірниках, ширинах вікна
  й кеглях.

## 1.2.2 — 20 вересня 2026

- **У стиснутій смужці фонограми знову є смуга перемотування.** Назва, час і
  смуга живуть усередині картки треку, а картку ховало разом зі списком —
  тому минула правка нічого не змінила (власник: «ничего не изменилось,
  полосы воспроизведения нет»).
- **Повернувся вибір вигляду тексту в пісеннику**: над списком куплетів — ті
  самі кнопки «в одну лінію» / «повністю», що й у Біблії.
- **Мінусовки, які не відкривалися.** Трапляється MP3, якого звичайний шлях
  не бере (довгі теги, обкладинка, чуже розширення): тепер програма спершу
  розкодовує такий файл засобами системи й лише потім кличе ffmpeg. Готове
  лежить у кеші під сталим іменем — раніше воно було щоразу нове, і та сама
  фонограма перетворювалася знову при кожному запуску.
- **У «Вікна слайда» є всі три кнопки** — закрити, згорнути, розгорнути.
  Хрестик не вбиває показ: ховає вікно, а наступний слайд вертає його.
- **«Довідка» → «Перевірка сумісності» показує й мережу**: хто слухає, на
  яких портах, що відповів сервер сторінок і де лежить тека `RemoteAPI`.

## 1.2.1 — 20 вересня 2026

- **Куплети більше не обрізаються.** Висота рядка рахувалася по ширині
  таблиці, а малювався рядок по ширині стовпця — на 32 точки вужчій. На цій
  різниці останній рядок куплета зрізало (власник прислав знімок). Тепер
  міряємо тією самою шириною, якою малюємо.
- **Тека даних — кнопкою.** Значок теки став у верхньому ряду вікна, поруч із
  повзунком кегля; пункт меню тепер і українською — «Тека даних у Finder…».
- **Стиснута панель фонограм лишає смугу перемотування** (власник: «при
  схлопывании плеера минусовок, полоса прокрутки должна остаться»), а нижня
  межа більше не забирає місця в панелі.
- **«Довідка» → «Перевірка сумісності»**: вікно каже, що на цій системі
  працює, а що ні, і кладе звіт у буфер обміну — щоб із macOS 11 можна було
  надіслати причину, а не здогадуватися.

## 1.2 — 20 вересня 2026

- **Гортання більше не виводить у зал саме по собі.** Власник: «вывод не
  должен происходить, пока не будет нажата кнопка показать или двойной щелчок
  мыши по пункту или клавиша enter». Тепер до першого показу стрілки міняють
  лише передпоказ; щойно слайд показано — гортають зал, як завжди, і так до
  «Сховати», «Чорний» чи Esc. Пульт доповідача й телефон виводять як раніше.
- **Пошук не зважає на розділові знаки й шукає словами.** «Спаси, Боже»
  знаходиться за «спаси боже», кома в запиті більше не заважає, а слово
  шукається з початку слова: «рад» знаходить «Радість» і не чіпає «страждання».
- **Папка даних — із меню.** «Налаштування» → «Папка данных в Finder…»: там
  переклади, пісенники, фони, шаблони, плани й налаштування.
- **Фонограми.** Панель стискається в один ряд і тягнеться за обидва краї,
  між списком і карткою треку з'явилася рухома межа. Додано формати, яких
  macOS сама не грає (ogg, opus, wma, ape, wv, amr та інші): такий файл
  перетворюється на льоту, а якщо перетворити нічим — програма каже про це
  словами, а не мовчить.
- **Куплети в списку** більше не займають зайвої висоти: рядок міряється за
  своїм текстом, а текст переноситься, а не обрізається одним рядком.
- **Фонограма з пульта.** Нові команди `/api/backing`: список, відтворення,
  гучність і тон — ними керує «Проповідник Слова» 1.3.0 (вкладка «Пісні»).
- **«Проповідник Слова» 1.3.0**: фонограми в піснях і видима указка — вимикач,
  колір, розмір, яскравість і «зняти наближення» під залом.
- **«Пульт Слова» 2.4**: підпис слайда переїхав на власний рядок, кнопки
  «Фото…» і «Файл…» стали рівно навпіл.

## 1.1.2 — 20 вересня 2026

- **Веб-слайди знову показують те, що в залі.** Власник: «не работают веб
  слайды». Сторінка `slovo-slide` лишалася чорною за будь-якого вірша: у її
  скрипті була неоголошена змінна, і малювання падало на першому ж рядку —
  слайд приходив, а на екрані не з'являлося нічого. Заразом сторінку можна
  відкрити з прозорим тлом для OBS: `…/slovo-slide?overlay=1`.
- Нова перевірка «веб-слайди» дивиться не на пакет, а на результат: відкриває
  сторінку справжнім рушієм браузера й вимагає, щоб вірш на ній було видно.

## 1.1.1 — 20 вересня 2026

- **Виправлення випуску 1.1: клієнти вийшли під старими номерами.** У 1.1 пульт
  і планшет зібралися наново (рівні кнопки, пісенник, ресурси), але номери
  лишилися ті самі — 2.2 і 1.8, як у 1.0.2. Тому «Перевірити оновлення…» на
  телефоні чесно казав «новішої немає», а кнопки лишалися старими. Тепер
  «Пульт Слова» 2.3, «Планшет Слова» 1.9, «Проповідник Слова» 1.2.1 — оновлення
  доходить.
- `GitHub/package.sh` більше не дасть повторити: перед випуском він порівнює
  кожен файл із тим, що вже лежить у релізі з тим самим іменем, і зупиняється,
  якщо вміст інший, а номер той самий.

## 1.1 — 19 вересня 2026

- **Пульт: пісенник цілком, а не тільки пошук.** На вкладці «Пісня» тепер
  список пісень відкритого пісенника й кнопка «Пісенник…» — вибрати інший.
  Пісню видно за номером і назвою, дотик показує її куплети, «‹ Пісні»
  повертає до списку.
- **Пульт і планшет: ресурси програми з телефона.** Меню → «Переклади й
  пісенники…»: ті самі джерела, що й у вікні «Ресурси з GitHub…» на
  комп'ютері (свій каталог «Слова», «Цитата з Біблії», MyBible, eBible.org,
  SoftProjector), пошук за назвою, вибір кількох і «Завантажити в «Слово»».
  Качає сама програма — файли одразу лягають на місце, а телефон бачить хід.
- **Зв'язок повертається сам.** Власник: «на короткое время иногда находит
  слово, но вскоре связь пропадает и не подключается снова». Причина — стара
  адреса: комп'ютер отримав від роутера іншу (або пульт занесло на адресу
  Parallels/VPN). Тепер після кількох невдач пульт і «Проповідник» шукають
  «Слово» заново й самі переходять на адресу, яка відповідає.
- **Пошук «Слова» з телефона — чотирма шляхами.** До Bonjour і розсилки
  додано ім'я `slovo.local` і перебір своєї підмережі (стук у порт 8103), як
  у «Проповідника»: тепер програма знаходиться й там, де точка доступу глушить
  і розсилку, і Bonjour.
- **Кнопки пульта вирівняно.** Власник: «кнопки в пульте выглядят криво, и не
  симметрично». Однакові поля в обох рядів, підписи в один рядок («Чорний»
  замість «Чорний екран», «Слайди» замість «Презентація», «Фото…» і «Файл…»
  однакової ширини) — ряди більше не розповзаються.
- «Пульт Слова» 2.2, «Планшет Слова» 1.8.

## 1.0.2 — 19 вересня 2026

- **Пульт, планшет і «Проповідник» оновлюються самі.** Власник: «программы не
  обновляются автоматически». Досі нову версію клієнтів треба було ставити
  руками; тепер кожна програма раз на добу питає GitHub і, якщо вийшла новіша,
  пропонує оновитися. Android завантажує новий `.apk` і віддає його
  встановлювачу (уперше попросить дозволити встановлення з цієї програми),
  «Проповідник» для Windows завантажує новий `.exe`, підміняє себе й
  відкривається вже оновленим. Перевірити самому — «Перевірити оновлення…» у
  меню програми; там же видно поточну версію. «Слово» для macOS так уміло й
  раніше: воно питає GitHub при запуску, кожні пів години й при поверненні до
  вікна.
- «Пульт Слова» 2.2, «Планшет Слова» 1.8, «Проповідник Слова» 1.2.0.

## 1.0.1 — 19 вересня 2026

- **Пульт на телефоні 2.1: адреса віршів без зайвого пробілу.** Рядок «У залі:»
  на телефоні писав «3:16-17, 19», а програма — «3:16-17,19». Тепер однаково.

## 1.0 — 19 вересня 2026

- **Адреса віршів: підряд — через тире, окремі — через кому.** Було «3:16,17,19»
  для будь-якого набору; тепер «3:16-17,19»: тире позначає суцільний проміжок,
  кома — вірш, що стоїть окремо. Адреса однакова скрізь — у залі, у передпоказі,
  у плані, в Історії, на планшеті, у пульті й у «Проповіднику».
- **Списки віршів і куплетів більше не накладаються при запуску.** Перші рядки
  зрідка малювалися один на одному, доки не перемкнеш пісню чи розділ: програма
  міряла їх до того, як список отримував свою ширину. Тепер висоти
  перемірюються, щойно ширина змінилася, а за чверть секунди після
  перезавантаження список перевіряє себе сам і переміряє, якщо рядкам було
  замало місця.
- **«Проповідник Слова» для Windows 1.1.0 — робоче місце, а не лише план.**
  Тепер це те саме, що планшет проповідника: сім вкладок, як у програмі
  (Біблія, Пісні, Презентація, Медіа, Зображення, Екран, Текст), зал живою
  картинкою з указкою й наближенням, план і Історія збоку, передпоказ і показ,
  «Назад», «Далі», «Показати», «Сховати», «Чорний», «Порожній», «Повернути план
  служіння». Файл із цього комп'ютера (презентація, фото) їде в «Слово» прямо з
  вкладки. Пошук «Слова» в мережі — трьома шляхами (розсилка, ім'я `slovo.local`,
  перебір підмережі), тож програма знаходить «Слово» й тоді, коли брандмауер
  глушить розсилку.

## 0.99 — 16 вересня 2026

- **«Проповідник Слова» для Windows** — функції планшета для проповідника на
  комп'ютері з Windows 10/11. План проповіді складається вдома, без зв'язку:
  своя бібліотека перекладів і пісенників (зі «Слова», з GitHub, з файла —
  «Цитата з Біблії», MyBible, `.vbm`, `.songbook`), вірші, пісні, файли й
  оголошення, кілька планів. На служінні — «⬆ Завантажити в «Слово»», і план стає
  головним; далі керування показом: зал картинкою, пункти плану, «Назад»,
  «Далі», «Показати», «Сховати», «Чорний екран», «Повернути план служіння».
  Один файл `Propovidnyk-Slova-1.0.0.exe`, встановлювати нічого не треба.
- **Пісня з плану проповіді лишається піснею.** Пісенник, який планшет чи
  «Проповідник» називали старим іменем «….vbm», програма не впізнавала (у
  бібліотеці він давно «….songbook») і показувала пісню простим текстом.

## 0.98 — 16 вересня 2026

- **Індикатор рівня фонограми — м'який і плавний.** Замість різких сегментів
  — суцільна заокруглена смуга з м'яким переходом зелене → жовте → червоне.
  Рівень піднімається м'яко й опускається повільно, 60 кадрів на секунду, без
  смикання між порціями звуку; найвищий пік — тонка риска, що плавно гасне.
- **Новий пісенник «Пісні церкви християн-баптистів ВІДРОДЖЕННЯ»** (112 пісень)
  — у «Ресурсах з GitHub…», вкладка «Пісенники». Це пісні, які церква співає
  й розучує як нові; раніше вони були дописані в кінець «Песнь Возрождения
  3055» і збивали нумерацію. У ресурсі «Песнь Возрождения 3055» тепер лише
  3066 пісень самого збірника.

## 0.97 — 16 вересня 2026

- **Смуга гучності фонограми — коротка.** На всю ширину вікна вона
  розтягувалася на пів екрана; тепер стоїть одразу після тону, сталої ширини,
  зі значками тихо/гучно з обох боків.

## 0.96 — 16 вересня 2026

- **Фонограма переїхала на «Пісні»** — панель стоїть над закладками
  пісенників, на всю ширину вікна. Під фонограму співають пісню, і ходити за
  нею на «Медіа» посеред служіння більше не треба.
- **Висота панелі тягнеться**: потягніть межу над нею вгору або вниз;
  подвійне клацання по межі повертає, як було. Висота запам'ятовується.
- На всю ширину панель розкладається зручніше: список фонограм ліворуч, картка
  треку з хвилею й рівнем — праворуч, під нею один ряд: грати, пауза, стоп,
  по колу, «після кінця — наступна», тон і гучність.

## 0.95 — 16 вересня 2026

- **Після переїзду даних програма лишається цілою.** Підпис накладено на весь
  пакет разом із вмістом, тож дані, що виїжджають із уже підписаного пакета,
  ламали його печатку — macOS могла сказати «пошкоджено». Тепер, коли
  оновлення зі старої версії таки принесло дані всередині програми, вона один
  раз оновлює свій підпис після переїзду. Далі переносити нічого й підпис
  більше не міняється.
- Повний випуск із ресурсами віддає їх окремим архівом для теки з даними, а не
  всередині програми — з тієї самої причини.

## 0.94 — 16 вересня 2026

- **Дані живуть поза програмою** — у теці `~/Library/Application
  Support/Slovo`: переклади, пісенники, фони, шаблони, шрифти, плани служінь,
  налаштування та історія. Досі вони лежали всередині «Слово.app».
- **Дозволи macOS переживають оновлення.** Раніше оновлення переносило дані зі
  старого пакета в новий і мусило переписати його підпис — macOS вважала
  «Слово» новою програмою й заново питала дозвіл на записування екрана. Тепер
  переносити нічого, підпис не міняється, дозволи лишаються.
- **Повну адресу теки з даними написано в програмі**: «Параметри» → «Шляхи»
  (адреса, «Показати у Finder», «Скопіювати шлях»), «Параметри» → «Модулі»
  (адреса теки модулів), підказки вікна ресурсів і майстра імпорту, а в
  довідці — новий розділ «Де лежать дані».
- **Перше відкриття 0.94 переносить старе саме**: дані зі старого пакета
  переїжджають у цю теку поелементно, нічого не перезаписуючи; те, що вже
  було вашим, лишається на місці. Залишки VisioBible (`Help`, `Language`,
  `Styles`, `ScreenShots`, службові ini) не переїжджають зовсім.
- Щоб зберегти про запас або перенести все на інший комп'ютер, досить
  скопіювати цю одну теку.
- Випуски на GitHub тепер підписані тим самим сертифікатом, що й збірка
  власника, — дозволи не скидаються й після встановлення нового випуску.

## 0.93 — 15 вересня 2026

- **Перевірка дозволів macOS під час запуску.** Програма сама дивиться, чи
  дозволено записування екрана й системного звуку (вкладка «Екран», звук
  потоків у NDI), локальну мережу (пульт, планшет, NDI) і вхідні з'єднання,
  коли ввімкнено брандмауер. Чого бракує — показує вікно «Дозволи macOS» зі
  станом кожного дозволу й кнопкою, що відкриває потрібний розділ «Системних
  параметрів»; після повернення стан оновлюється сам. Вікно відкривається й
  з меню «Довідка», а у вкладці «Екран» при браку дозволу є кнопка «Дати
  дозвіл…». Після кожного оновлення macOS може спитати дозвіл на запис екрана
  знову — вікно одразу про це скаже.
- **Про нову версію видно без ручної перевірки.** GitHub питаємо кожні 30
  хвилин і щоразу, як людина повертається до програми (досі — раз на 12
  годин). Пропозиція — звичайне вікно поверх інших, а не модальне, що ховалося
  за вікнами; поки нову версію не встановлено, у верхньому рядку стоїть
  кнопка «Оновлення …».

## 0.92 — 15 вересня 2026

- **Перемикання вкладок не міняє слайд у залі.** Досі, поки проектор не
  погашено, перехід, наприклад, з Біблії на Пісні одразу перемальовував вірш
  у залі шаблоном пісень. Тепер зал лишається таким, як його вивели, а шаблон
  іншої вкладки з'являється разом зі слайдом, виведеним із неї. Підписи під
  слайдом (назва пісні, перекладів) теж беруться з миті виводу.
- **Кнопка без значка в плеєрі** — вибір звукової доріжки показував лише
  стрілку; тепер на ній значок. Самоперевірка стежить, щоб кнопок без значка
  й підпису не лишалося.

## 0.91 — 15 вересня 2026

- **Фонограми — свій список.** Музика, перетягнута на панель фонограм, іде до
  їхнього списку, а не до списку плеєра з заставками й роликами. Кнопки
  «додати», «прибрати», «очистити»; подвійне клацання — грати; список
  пам'ятається між запусками.
- **Хвиля треку, як в Audacity**: увесь трек на темній картці, зіграна частина
  кольором, курсор там, де звук; клацання чи протягування — грати з цього
  місця.
- **Пік-метр** лівого й правого каналів (зелене, жовте, червоне).
- **Окрема кнопка «Пауза»** поруч із «Грати» і «Стоп».
- **«Після кінця — наступна»**: доіграла фонограма — пішла наступна зі списку.
- **Тон ±0,5 (півтону)** до ±3 тонів без зміни темпу; тон запам'ятовується для
  кожного файлу.
- Панель фонограм — у стилі програми; висоту панелі (і її списку) тягнуть
  мишею, ширину всього блоку — теж.
- Довідка: розділ «Програма не відкривається» прибрано — прочитати його там,
  де програма не запускається, неможливо; інструкція із запуску лишається на
  GitHub. У довідці — про фонограми.
- Інструкція із запуску на GitHub — зі справжніми знімками вікон macOS.

## 0.90 — 15 вересня 2026

- **Галочка «Активна»** у «Керуванні» (за умовчанням увімкнена): стрілки
  гортають слайд одразу в залі, як досі. Зняли — стрілки гортають лише
  передпоказ, а в зал слайд виводять «Показати» чи Enter. Пульт доповідача
  гортає зал завжди.
- **Підписи «Керування» видно цілком** — «Поточний», «Знімок», англійські й
  німецькі підписи більше не обрізаються: кнопки беруть ширину за підписом.
- **Історія не міняється від показу з неї**: пункт, виведений з Історії в
  зал, більше не з'являється нагорі як новий запис.
- **Довідка в самій програмі** українською й англійською, без інтернету:
  меню «Довідка» (⌘?) — кожна вкладка, гарячі клавіші, «Якщо щось не так»,
  пошук і перемикач мови.
- **Спливні підказки** до кнопок, списків і перемикачів головного вікна,
  параметрів, ресурсів та імпорту — і їхні переклади англійською та німецькою.
- **Інструкція із запуску на Mac** на GitHub: блокування macOS для програм не
  з App Store, усі повідомлення про помилки й що з ними робити, дозволи, які
  просить програма.
- Опис на GitHub: сумісність із VisioBible.
- **Смужка завантаження ресурсів рухається** під час завантаження, а не
  стрибає лише між файлами.
- **Пульт у браузері на телефоні**: рядок стану в шапці знову видно цілком —
  шапка у два ряди (з кнопкою мови в 0.88–0.89 він стискався до однієї літери).
- Опис програми на GitHub: знімки екрана, усі формати імпорту, джерела
  ресурсів, ліцензії, англійська.

## 0.89 — 15 вересня 2026

- **Переклади з eBible.org**: ~1300 вільних перекладів понад тисячею мов, серед
  них англійські й українські. Ставляться з вікна ресурсів модулем MyBible.
- **Пісенники SoftProjector** у вікні ресурсів: українські («Євангельські
  пісні», «Пісні спасенних», «Псалмоспіви»), російські, англійські, чеські,
  словацькі, німецькі. Програма читає й найстаріший текстовий формат `.sps`.
- **Вікно ресурсів — вкладками** «Переклади Біблії», «Пісенники», «Фони й
  шаблони»: у кожної свої джерела, переклади й пісенники не змішуються.
- **Історія — як План**: клацання лише виділяє запис, у зал виводять подвійне
  клацання або Enter.
- **«Про програму»** показує версію й має кнопку «Перевірити оновлення».
- **Оновлення з програми, запущеної просто з архіву**, більше не падає з
  «том доступний лише для читання»: macOS запускає таку програму з тимчасової
  копії (App Translocation), і оновлення тепер замінює саму програму там, де
  вона лежить.
- **Вікно оновлення — у стилі заставки**: смужка всього оновлення й рядок того,
  що робиться зараз; докладний журнал — у щоденнику програми.
- **Про нову версію програма повідомляє** на кожному запуску (і раз на 12
  годин, поки відкрита); вимкнути — «Ніколи» в налаштуваннях оновлення.

## 0.88 — 15 вересня 2026

- **Інтерфейс українською до кінця**: близько ста десяти підписів, що лишалися
  російськими (вікно «Параметри» — Слайд, Медіа, Remote API, Основні,
  Додаткові, Оновлення, Гарячі клавіші; Конструктор, майстер імпорту, медіа).
- **Мова за умовчанням — за мовою системи**: українська — українська,
  російська — російська, будь-яка інша — англійська. Так само в пульті,
  веб-пульті й планшеті.
- **Англійська в пульті, веб-пульті й планшеті** з перемикачем: у браузері —
  кнопка в шапці, в програмах для Android — «Мова / Language…» у меню.
  «Пульт Слова» 2.0, «Планшет Слова» 1.7.
- **«Мій перегляд» не ховає початок довгого тексту** — ні в браузері, ні на
  планшеті.
- **Підказки з форматами імпорту** в майстрі імпорту, у вікнах вибору файлів і
  в меню імпорту пісенника.
- Реєстр MyBible: назва в кілька рядків не залазить на сусідній рядок списку.
- Ресурси «Слова»: у перекладів і пісенників з'явилася мова — пошук «uk»,
  «ru», «en» знаходить їх; назви перекладів Куліша й Хоменка — «Українська
  Біблія» (було «Українская»).

## 0.87 — 15 вересня 2026

- **Після зміни перекладу список віршів показує новий переклад.** Книгу
  нового перекладу програма читає у фоні, і список лишався з тексту
  попереднього: вибрано Турконяка — у списку Синодальний, а в передпоказ і в
  зал іде Турконяк. Тепер список перечитується, щойно книга прочитана.
- **Фони за умовчанням — як у повній збірці**: загальний фон — хрест, фон
  слайда — чорний. Для нової установки; вибраних раніше фонів це не змінює.

## 0.86 — 15 вересня 2026

- **Системні підписи українською** на українській macOS: вікно вибору файлу
  («Скасувати», «Вибране»), пункти «Автозаповнення», «Емодзі та символи»,
  «Відмінити «Вставлення»», розміри «МБ». Пакет не оголошував своїх мов, і
  macOS показувала все це англійською.
- **Опис оновлення у вікні читається суцільно**: абзаци й пункти більше не
  рвуться посередині речення.
- **Майстер імпорту після перенесення відкривається спочатку**, а не старим
  зведенням із кнопкою «Імпортувати».
- У розділі «Редагування» системні пункти не повторюються.

## 0.85 — 15 вересня 2026

- **Вставка й копіювання в полях працюють**: ⌘V, ⌘C, ⌘X, ⌘A і ⌘Z. У
  рядку меню з'явився розділ «Редагування» — без нього macOS не передає цих
  клавіш жодному полю (пошук, назва пісні, текст оголошення, «Перейти до
  теки» у вікні вибору файлу).
- **Вибрана перша пісня справді не ховається** під полем «Номер або назва
  пісні»: вкладку будували ще без висоти, і список з'їжджав на пів рядка.
  Виправлення в 0.84 цього не зачепило.
- **Імпорт бачить архіви модулів у вибраній теці**: модулі «Цитати з
  Біблії», завантажені zip-архівами (тека завантажень), тепер у списку
  майстра — з обгорткою-текою й без неї; чужі zip пропускаються.
- **Тека лише з пісенниками .songbook** приймається майстром імпорту (була
  «нічого імпортувати»).
- Майстер імпорту: під заголовком «Крок 2 з 6» замість номерів посібника
  VisioBible, колонки «Версія» і «Шлях» українською.

## 0.84 — 15 вересня 2026

- **Оновлення з 0.82 і 0.83 могло прибрати програму**: вікно ходу при
  закритті програми стирало вже готовий новий пакет, і на місці «Слово.app»
  лишалося тільки «Слово.app.old». Виправлено: готове оновлення не
  скасовується нічим, заміна йде з відкатом (не вдалася — старий пакет
  вертається на місце), дані переносяться лише після успішної заміни.
  **Якщо ви на 0.82 чи 0.83 — завантажте 0.84 з цієї сторінки вручну**, а не
  через меню. Якщо програма вже зникла — перейменуйте «Слово.app.old» назад
  у «Слово.app».
- **Модуль «Пісні» українською** і в збірці без файлів мови VisioBible: «Усі
  пісні», «Пошук пісні», поля картки пісні, повідомлення редактора.
- **Вибрана перша пісня не ховається** під полем «Номер або назва пісні».

## 0.83 — 15 вересня 2026

- **Переклади з реєстру MyBible з апострофом чи кирилицею в назві
  завантажуються** («Біблія в пер. Івана Огієнка, 1962», «ФІЛ», «МСЦ'22»…) —
  раніше сервер відповідав 404.
- **Пошук за кодом мови** у вікні ресурсів («uk», «ru», «en») показує лише
  цю мову, а не всі назви з такими літерами.
- **Конструктор не питає «Зберегти?»**, якщо його відкрили й закрили без змін
  (коли шаблонів пісень ще нема і редактор створив новий сам).

## 0.82 — 14 вересня 2026

- **Оновлення програми з меню тепер доходить до кінця.** У 0.69–0.81 вікно
  «Завантажую…» висіло, хоч файл уже завантажено: програма не закривалася й
  не замінювала себе. (Хто на 0.8 чи 0.81 застряг у такому вікні — натисніть
  у ньому «Скасувати»: програма закриється, оновиться й відкриється знову.)
- **Вікно оновлення з докладним ходом**: яка версія зараз і на GitHub, файл і
  розмір, завантаження з відсотками та швидкістю, розпакування, перевірка
  нового пакета, що перейде зі старого пакета, запуск помічника заміни, відлік
  до перезапуску. «Скасувати» справді скасовує.
- **Фони й картинки шаблонів не губляться**, коли програму перенесли в іншу
  теку чи ресурси завантажено в Application Support: застарілі повні шляхи
  в налаштуваннях і шаблонах знаходять ті самі файли в поточних теках даних.
- Опис нової версії у вікні — звичайним текстом, без розмітки.

## 0.81 — 14 вересня 2026

Виправлення після перевірки скачаної з GitHub 0.8. Ставиться оновленням
з 0.8 чи 0.69: «Настройка» → «Перевірити оновлення програми…».

- **Пісенники з ресурсів «Слова» не лягали на диск**: «встановлено», у списку
  є ім'я, а файла нема. У архівах файлових ресурсів (пісенники, модуль
  MySword) файл лежав у теці «Modules/», і програма ставила його ТЕКОЮ.
  Архіви ресурсів переіздано; програма тепер знаходить вміст архіву за
  будь-якого пакування, а те, що вже лягло текою, лагодить сама на запуску.
- **Вікно «Відкриваю бібліотеку…» висіло поверх усіх вікон**, коли
  перекладів ще нема, і закривало половину вікна ресурсів — ховається, щойно
  бібліотеку прочитано.
- **Список модулів у налаштуваннях показує те, що справді є**: імен
  перекладів і пісенників, яких нема на диску, більше не видно; завантажене
  з ресурсів з'являється в списку; пісенник, що став `.songbook`, — під новим
  ім'ям із тією самою галочкою.
- Перемикач «Переклади Біблії · N / Пісенники · N» у налаштуваннях більше не
  порожній при першому відкритті.
- У порожньому головному вікні — кнопка «Завантажити з GitHub…».
- «Позначити всі нові» й «Позначити оновлення» у вікні ресурсів діють на
  знайдене пошуком (у реєстрі MyBible — тисячі перекладів).

## 0.8 — 14 вересня 2026

**Лише програма — ресурси з інтернету.** Архів програми більше не везе
переклади, пісенники, фони й шаблони: вони лежать окремо в
[roman885-85/slovo-resources](https://github.com/roman885-85/slovo-resources) і
завантажуються з самої програми — з вибором і оновленнями.

- **Перший запуск** пропонує завантажити переклади й пісенники з GitHub
  («Ресурси з GitHub…») або імпортувати свої (VisioBible, «Цитата з Біблії»,
  MyBible, тека чи архів).
- **«Настройка» → «Ресурси з GitHub…»** — три джерела: ресурси «Слова»
  (56 перекладів, 22 пісенники у форматі `.songbook`, фони, шаблони, шрифти,
  сторінки), модулі «Цитата з Біблії», реєстр MyBible. Пошук, групи за родом
  чи мовою, «Позначити всі нові», «Позначити оновлення».
- **Оновлення з 0.69 на 0.8** — пунктом «Перевірити оновлення програми…»:
  переклади й дані з пакета 0.69 переходять у 0.8.
- Повний випуск із ресурсами всередині — [0.69](https://github.com/roman885-85/slovo/releases/tag/v0.69).

## 0.69 — 14 вересня 2026

**Повний випуск — з усім пакетом ресурсів усередині програми:** 56 перекладів
Біблії, 22 пісенники, фони слайдів, шаблони, шрифти й сторінки веб-слайдів.
Розпакували — і все вже на місці, нічого не треба завантажувати.

### Ресурси з інтернету
- **«Настройка» → «Ресурси з GitHub…»** — вікно, де вибирають, що завантажити,
  і з якого джерела:
  - ресурси «Слова» — [roman885-85/slovo-resources](https://github.com/roman885-85/slovo-resources):
    переклади, пісенники, фони, шаблони, шрифти, сторінки;
  - модулі «Цитата з Біблії» — [BibleQuote/BibleQuote-Modules](https://github.com/BibleQuote/BibleQuote-Modules);
  - реєстр MyBible — тисячі перекладів багатьма мовами, з пошуком і за мовами.
  Позначити «всі нові» чи «оновлення» й завантажити; уже встановлене
  позначено «є», змінене в каталозі — «оновлення».
- **Перший запуск без перекладів** пропонує завантажити їх з GitHub або
  імпортувати з комп'ютера.

### Оновлення програми
- Програма сама перевіряє на GitHub, чи вийшла нова версія (не частіше за
  «Інтервал перевірки оновлень» у «Параметрах»), і пункт меню «Перевірити
  оновлення програми…». Оновлення завантажує нову версію, замінює програму й
  перезапускає її; **переклади, пісенники, фони й шаблони з пакета переходять
  у нову версію**. Після оновлення можна одразу оновити й ресурси.
- Версії порівнюються як десяткові: 0.8 новіша за 0.69.

### Інше
- Пісенники в пакеті — уже у форматі «Слова» `.songbook`.
- «Довідка» відкриває документацію на GitHub.

## 0.68 — 14 вересня 2026

### План
- **Одинарне клацання лише виділяє пункт; відкривають його подвійне клацання
  або Enter** — так вирішив власник (у 0.66–0.67 відкривало й одинарне).

### Свій формат пісенника
- **`.songbook`** — власний формат пісенника «Слова»: JSON в UTF-8, читається
  будь-яким редактором. Програма пише лише його; `.vbm` VisioBible лишається
  форматом імпорту (майстер перетворює `.vbm` у `.songbook`, `.vbi` більше
  не потрібен) та експорту (планшет отримує `.vbm`, зібраний на льоту).
  Коли поруч лежать `X.songbook` і `X.vbm`, береться свій. План, Історія і
  галочки модулів знаходять збірник і за старим ім'ям `X.vbm`.

### Без залишків VisioBible
- **Переклади й модулі — завжди в пакеті** («Слово.app/Contents/Resources/app/Modules»):
  скопійований пакет цілий. Пакет більше не містить довідки, мов, стилів,
  знімків і службових файлів VisioBible; довідка — своя, мова інтерфейсу —
  зі свого словника (українська, російська, англійська, німецька).
- **Пісенники `.vbm` в теці модулів один раз переводяться у `.songbook`**,
  оригінали відкладаються в «Імпорт з VisioBible» поруч; програма про це
  повідомляє.
- **У налаштуваннях переклади й пісенники — окремими списками** з
  перемикачем над списком; «Позначити все» діє на відкритий список.

### Android
- Програми не змінювалися: «Пульт Слова» 1.9, «Планшет Слова» 1.6.

## 0.67 — 13 вересня 2026

### Конструктор слайдів
- **Два редактори — Біблії та пісень** («Редактор: Біблія / Пісні»). У
  кожного свій список шаблонів, вони не перетинаються; шаблон несе
  прапорець «для пісень» у файлі. Новий шаблон у редакторі пісень — куплет
  і підпис частини («Куплет», «Приспів»), без адреси й другого перекладу.
  Меню «Шаблон» на панелі керування показує лише шаблони свого розділу.
- Полотно показує своє: у редакторі Біблії — вірш з адресою навіть при
  відкритій вкладці пісень, у редакторі пісень — куплет і підпис частини.
- Новий, ще не збережений шаблон видно в списку як «… (не збережено)».
- Два однойменні шаблони більше не зливаються в один пункт списку — доти
  вибір по номеру відкривав не той шаблон.

### Розміри блоків
- **Ширину Плану, Історії (а з ними й передпоказу) тягнуть роздільниками**;
  такі ж роздільники на вкладках Презентація (файли | сторінки |
  передпоказ), Зображення, Медіа, Екран і в Конструкторі (список об'єктів |
  полотно | панелі). Ширини пам'ятаються; подвійне клацання по роздільнику
  повертає автоматичну. На ручці висоти нижнього ряду тепер видно три крапки.

### Списки
- **Один кегль у всіх рядках Плану й Історії**: довгі рядки більше не
  дрібнішають, зайве ріжуть три крапки.

### Наближення
- **Повернення з наближення й «підтягування» сторінки йдуть плавно** (0,28 с),
  а не стрибком — у програмі, на телефоні, планшеті й у пульті в браузері.
  Під щипком і колесом — одразу, щоб не відставати від пальців.

### План і пісні
- **Пісня з Плану стає поточною у вкладці пісень**: список і частини
  переходять на неї, стрілка й кнопки частин із пультів листають саме її.
  Доти в залі була пісня з Плану, а у вікні — попередня, і стрілка листала
  попередню.

### Презентації
- **Фон теми** (текстура з дуотоном, суцільний із відтінком, градієнт) —
  раніше такий слайд був білим.
- **Маркери списків**: галочки Wingdings, дефіси, автонумерація — кеглем і
  кольором тексту, з відступами і успадкуванням із макета й зразка.
- **Текст не стискається під рамку там, де PowerPoint дає йому вийти за неї**
  (`noAutofit`), — кегль і переноси рядків збігаються з PowerPoint.

### Android
- «Пульт Слова» 1.9 і «Планшет Слова» 1.6: плавне повернення з наближення.

## 0.66 — 13 вересня 2026

### Поля вводу
- **Курсор видно в усіх полях**, і набирається все слово, а не перша літера.
  Причин було дві: повторне взяття фокуса на кожну звірку стану виділяло
  набране, і наступна літера його затирала; а перемальовування поля щоразу
  скидало курсор. Стосується пошуку й адреси в Біблії, полів книги, розділу,
  вірша, швидкого вибору пісні й пошуку частини за текстом.
- Порожні поля підказують, що сюди набирати («Назва книги…», «Номер або назва
  пісні…», «Слова з тексту пісні…»).

### План та Історія
- **Пункт плану відкривається одинарним клацанням** — як на планшеті й в
  оригіналі; доти потрібне було подвійне, і здавалося, що План «спрацьовує не
  завжди». Перетягнути пункт і вибрати кілька (Cmd/Shift) можна як і раніше.
- **Клацання по запису Історії показує його в зал**, а не лише відкриває в
  передпоказі — «повернутися до показаного», як на планшеті.

### Медіа
- **Гучність регулюється на всій довжині повзунка**: половина — −12 дБ,
  чверть — −24, а не «тихішає лише біля нуля». Те саме для фонограми.
- **Кнопки «Повтор», «Без звуку», «Автозапуск», «На екран»** видно, чи
  ввімкнені: увімкнена малюється натиснутою.

### Панель «Керування»
- Кнопки переходу підписані словами: «‹‹ Розділ», «‹ Вірш», «↺ Поточний»,
  «Розділ ››», «Вірш ›», «Знімок».

### Презентації
- Картинки розтягуються в рамку, як у PowerPoint (доти вписувалися з білими
  полями й чорною обвідкою).
- **Тіні** фігур, картинок і тексту — з файла й зі стилів теми.
- **Фон слайда** з теми (кремовий, а не білий), **прикраси зразка й розмітки**
  (орнаменти, лінії, емблеми), з'єднувальні лінії, сховані фігури
  пропускаються, табуляція в дюйм, розриви рядків і поля (номер слайда).
  Звірено з PDF, який зробив сам PowerPoint.

### Конструктор
- **Шаблон для пісень окремо**: у Конструкторі вибір «Для: Біблії й усього /
  Пісень»; на вкладці пісень меню «Шаблон» на панелі править шаблон пісень і
  має «Як для Біблії».


## 0.65 — 12 вересня 2026

Виправлення за зауваженнями власника до 0.6.

### Налаштування

- **Вкладка «Модулі» поділена на розділи** — «Переклади Біблії» й
  «Пісенники», у заголовку кожного видно «увімкнено N з M». Клацання по
  заголовку вмикає або вимикає весь розділ.
- **Галочка модуля діє одразу**, не чекаючи «Ок»: вимкнений переклад зникає
  зі смуги перекладів, вимкнений пісенник — зі списку пісенників. «Відмінити»
  повертає все, як було. Раніше розпис застосовувався лише тоді, коли в ньому
  лишався хоч один увімкнений модуль, і знята остання галочка мовчки не робила
  нічого.
- **Скидання налаштувань** — кнопка «↺ Скинути…»: окремо поточна вкладка або
  всі налаштування разом. Повертаються значення першого запуску; файли
  (модулі, пісенники, шаблони слайдів, плани) лишаються на місці, а
  «Відмінити» у вікні відкочує й саме скидання.
- **Перенесення налаштувань** — «Зберегти у файл…» і «Узяти з файла…»: усі
  налаштування одним файлом (параметри, розпис модулів, шляхи, розкладки
  клавіш, веб-сторінки, кольори частин пісень). Чужий файл відхиляється, і
  тоді нічого не міняється.
- **Enter у полі більше не зачиняє вікно.** Кнопка за умовчанням («Закрити» в
  Конструкторі, «Ок» у «Параметрах») перехоплювала Enter раніше за поле, у
  якому стоїть курсор, — і набране число зачиняло вікно. Тепер Enter лише
  закінчує правку поля.

### Конструктор слайда

- **Тінь м'якша й різноманітніша**: зсув і розмиття — дробові, повзунком і
  полем; новий **«Напрямок»** тіні (0…360°, зі стрілкою); чотири заготовки —
  «Без тіні», «Легка», «М'яка», «Глибока». Кут 45° — як було завжди, і старі
  шаблони не змінилися ні на піксель.
- **Прозорість контуру** — окремий повзунок; товщина контуру теж має повзунок.
- Вибір у списку того самого шаблону, що вже відкритий, більше не питає про
  збереження.

### Вигляд списків

- **Текст не губиться й не наповзає за межі рядка.** Висоту рядка задавали
  після зміни кегля, не питаючи його; плитка не росла з кеглем і рахувалася по
  чужій ширині, через що крайня плитка виїжджала за край списку; а сам рядок
  малював текст на всю потрібну висоту — поверх сусідів. Тепер рядок ніколи не
  буває нижчим за свій рядок тексту, плитка рахує крок від власної ширини, а
  те, що не вміщається, малюється одним рядком (у щільних списках Плану й
  Історії — по центру) і за свої межі не виходить.
- Пісенник перезбирає вигляд списків при зміні кегля — як це робила Біблія.

### Пульт і планшет

- **Підсвічений вірш іде за програмою** — у пульті в браузері, у пульті на
  телефоні («Пульт Слова» 1.8) і на планшеті («Планшет Слова» 1.5).
  Перемкнули вірш на комп'ютері, з іншого пульта чи гарячою клавішею — на
  сторінці підсвічується те саме місце, і список сам підводиться до нього.
  Свій щойно зроблений вибір при цьому не збивається.
- Так само переїжджає підсвічення пісні в переліку пісень.

### Мова

- Український інтерфейс без російських залишків: підписи «Параметрів», панелі
  пісень і назви дій гарячих клавіш, яких немає в перекладі автора, тепер
  беруться з нашого словника. Наші написи перекладено на англійську й
  німецьку повністю (1797 рядків, 100 %).


## 0.6 — 11 вересня 2026

### Нове

- **План проповіді** (програма «Планшет Слова» 1.3). Проповідник складає план
  на планшеті заздалегідь — удома, без зв'язку з програмою: вірші, пісні,
  презентації, картинки, відео, оголошення. Кілька планів наперед — кнопка
  «Плани».
  - **Бібліотека планшета** — свої переклади Біблії й пісенники для
    складання без зв'язку: «Зі «Слова»» (усе, що є в програмі), «З GitHub»
    (каталог модулів «Цитати з Біблії»), «З файла» (zip «Цитати з Біблії»,
    MyBible `.SQLite3`, пісенник VisioBible `.vbm`).
  - **«Завантажити в «Слово»»** — на служінні одна кнопка: файли плану їдуть
    у програму, план стає головним, керування показом переходить на планшет.
  - **План служіння не губиться**: на час проповіді його лише відкладено;
    «Повернути план служіння» (на планшеті й на панелі Плану в програмі)
    вертає його таким, яким він був. Автозбереження останнього плану на цей
    час не пише план проповіді поверх плану служіння.
  - **Автоімпорт модулів**: переклад чи пісенник із плану, якого в програмі
    немає, вона ставить собі сама (тим самим майстром імпорту) й одразу
    вмикає. Якщо переклад так і не знайшовся — вірші покажуться текстом, що
    привіз планшет; так само пісня без свого пісенника.
- **Пункт плану «Файл»** — презентація, картинка чи відео прямо в Плані;
  дотик по пункту відкриває файл на своїй вкладці й одразу показує в залі.
- **Мій перегляд** — на планшеті (меню ☰ → «Мій перегляд…») і в пульті в
  браузері (кнопка «Вигляд»): текстовий слайд крупно, у своєму оформленні —
  темне, світле, сепія, розмір на свій смак, — а не картинкою залу зі стилем
  стіни. Картинки й презентації завжди видно такими, як на стіні.
- **Мови інтерфейсу вбудовано**: українська (за умовчанням), російська,
  англійська, німецька — у меню мови й у вікні «Вибір мови», навіть без
  файлів перекладу VisioBible.
- **«Планшет Слова» на телефоні** попереджає, що зроблений для планшета, але
  працює: колонки «Вміст», «Зал», «План» перемикаються, вкладки програми
  прокручуються; на телефоні програма тримається вертикально (лежачи
  списку віршів не лишалося місця).
- **NDI вбудовано в збірку для завантаження** — разом із файлом ліцензій NDI.
- **Збірка для завантаження не порожня**: у ній вільні для поширення
  Синодальний переклад і переклад Куліша та два пісенники («Песнь
  Возрождения 2800» і «3300»).

### Виправлено

- Пошук за словами з планшета й пульта в браузері: перший пошук після
  запуску програми читав увесь переклад (13 с, на зайнятому комп'ютері —
  понад 20), і планшет писав «нічого не знайдено», а браузер так і лишався
  на «Шукаю…». Тепер програма готує пошук одразу після запуску (далі — за
  частки секунди), а планшет і браузер чекають, поки вона справді закінчить.
- На комп'ютері без файлів перекладу VisioBible (чиста установка) інтерфейс
  був російським: мова нашого словника не встановлювалася зовсім. Тепер —
  українська за умовчанням.
- 106 підписів вікон і меню не мали українського перекладу, ~108 —
  англійського й німецького; тепер перекладено всі.
- Англійський і німецький інтерфейс: 88 фраз (вікно «Пульт у браузері»,
  «Програми для Android» та інші) лежали в словнику мимо розділу, який читає
  програма, і не показувалися. Тепер показуються.

### Для розробників

- Нові запити пульта: `GET /api/library`, `/api/library/bible?id=`,
  `/api/library/songbook?file=`; `POST /api/upload?store=1`,
  `/api/sermon-plan`, `/api/sermon-end`, `/api/module-import?name=`.
  У стані — `sermon: {on, title}`.
- Самоперевірки: «Бібліотека для плану проповіді» і «План проповіді: головний
  на час проповіді» (розділ `--check=планшет`).
- Частину коментарів у коді перекладено українською.

## 0.5 — 11 вересня 2026

Перша публікація: програма для macOS, «Пульт Слова» 1.6 для телефона,
«Планшет Слова» 1.2, пульт у браузері.
